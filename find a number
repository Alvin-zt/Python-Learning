from math import sqrt
for number in range(99, 81, -1):
    root = sqrt(number)
    if root == int(root):
        print('{} is what you need'.format(number))
        break
else:
    print('Didn\'t find it')
